version: 2

models:
    - name: array_contains_test
      description: "tests array contains"
      columns:
          - name: value_col
            tests:
              - accepted_values:
                  values: [1]